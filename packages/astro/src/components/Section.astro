---
import { section } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  title?: string;
  className?: string;
};

const { title, className } = Astro.props;
const s = section();
const rootProps = mergeProps(s.root, className);
const titleProps = mergeProps(s.title);
---
<section {...rootProps}>
  {title ? <h2 {...titleProps}>{title}</h2> : null}
  <slot />
</section>
