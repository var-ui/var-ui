---
import { sideNav } from '@var-ui/core';
import { recipeProps } from '../utils';

type Props = {
  title: string;
  className?: string;
};

const { title, className } = Astro.props;
const s = sideNav();
---

<div {...recipeProps(s.sectionTitle, className)}>{title}</div>
