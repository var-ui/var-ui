---
import { sideNav } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  title: string;
  className?: string;
};

const { title, className } = Astro.props;
const s = sideNav();
---

<div {...mergeProps(s.sectionTitle, className)}>{title}</div>
