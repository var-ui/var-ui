---
import { topNav } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  label?: string;
  className?: string;
};

const { label = 'Top navigation', className } = Astro.props;
const s = topNav();
const hasCenter = Astro.slots.has('center');
const hasStart = Astro.slots.has('default');
---

<nav
  aria-label={label}
  data-var-ui-top-nav
  data-layout={hasCenter ? 'grid' : undefined}
  {...mergeProps(s.root, className)}
>
  {Astro.slots.has('heading') && (
    <div data-var-ui-top-nav-heading {...mergeProps(s.heading)}><slot name="heading" /></div>
  )}
  {hasStart && <div data-var-ui-top-nav-start {...mergeProps(s.start)}><slot /></div>}
  {hasCenter && <div data-var-ui-top-nav-center {...mergeProps(s.center)}><slot name="center" /></div>}
  {Astro.slots.has('end') && (
    <div data-var-ui-top-nav-end {...mergeProps(s.end)}><slot name="end" /></div>
  )}
</nav>
