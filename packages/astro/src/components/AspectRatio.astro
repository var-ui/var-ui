---
import { aspectRatio } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  ratio?: number;
  className?: string;
};

const { ratio, className } = Astro.props;
const rp = mergeProps(aspectRatio(), className);
---
<div {...rp} style={ratio ? `aspect-ratio: ${ratio}` : undefined}><slot /></div>
