---
import { button, resolveButtonProps, type ButtonVariantProps } from '@var-ui/core';
import { mergeProps } from '../utils';

interface Props extends ButtonVariantProps {
  className?: string;
  type?: 'button' | 'submit' | 'reset';
  disabled?: boolean;
}

const {
  intent,
  tone,
  appearance,
  size = 'md',
  elevated,
  className,
  type = 'button',
  disabled,
} = Astro.props;
const rp = mergeProps(
  button(resolveButtonProps({ intent, tone, appearance, size, elevated })),
  className,
);
---

<button type={type} disabled={disabled} {...rp}><slot /></button>
