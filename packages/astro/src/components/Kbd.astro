---
import { kbd } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  className?: string;
};

const { className } = Astro.props;
const rp = mergeProps(kbd(), className);
---
<kbd {...rp}><slot /></kbd>
