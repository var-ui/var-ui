---
import { steps } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<ol {...mergeProps(steps().root, className)}>
  <slot />
</ol>
