---
import { steps } from '@var-ui/core/steps';
import { recipeProps } from '../utils';

type Props = {
  className?: string;
};

const { className } = Astro.props;
---

<ol {...recipeProps(steps().root, className)}>
  <slot />
</ol>
