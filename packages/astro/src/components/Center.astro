---
import { center } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  inline?: boolean;
  className?: string;
};

const { inline = false, className } = Astro.props;
const rp = mergeProps(center({ inline: inline ? 'true' : 'false' }), className);
---
<div {...rp}><slot /></div>
