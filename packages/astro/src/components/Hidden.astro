---
import { hiddenClassName, type HiddenMap } from '@var-ui/core';
import { mergeProps } from '../utils';

type Props = {
  hide?: boolean | HiddenMap;
  as?: string;
  className?: string;
};

const { hide, as = 'div', className } = Astro.props;
const rp = mergeProps(hiddenClassName({ hide }), className);
const Tag = as;
---
<Tag {...rp}><slot /></Tag>
