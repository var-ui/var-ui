import { atProperty, type TokenSchema } from 'typestyles';

export const spaceTokenSchema = {
  0: atProperty.length,
  1: atProperty.length,
  2: atProperty.length,
  3: atProperty.length,
  4: atProperty.length,
  5: atProperty.length,
  6: atProperty.length,
  7: atProperty.length,
  8: atProperty.length,
  9: atProperty.length,
  10: atProperty.length,
  11: atProperty.length,
  12: atProperty.length,
  16: atProperty.length,
  20: atProperty.length,
} as const satisfies TokenSchema;
