---
import DocsToc from '@var-ui/docs/DocsToc';

type Props = {
  contentSelector?: string;
  headingSelector?: string;
};

const { contentSelector, headingSelector } = Astro.props;
---

<DocsToc contentSelector={contentSelector} headingSelector={headingSelector} />
