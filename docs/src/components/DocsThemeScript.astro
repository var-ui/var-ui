---
/**
 * Site adapter — import kit components via relative path so Netlify SSR
 * (Node ESM) does not load workspace `.astro` through package exports.
 */
import DocsThemeScript from '../../../packages/docs/src/components/DocsThemeScript.astro';
---

<DocsThemeScript />
