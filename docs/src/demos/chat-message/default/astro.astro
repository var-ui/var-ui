---
import { avatar, chatMessage, chatMessageBubble, stack, textBlock } from '@var-ui/core';
import { mergeProps } from '../../../lib/mergeProps';

// No @var-ui/astro ChatMessage — core recipe row approximating React layout.
const m = chatMessage({ sender: 'assistant' });
const bubble = chatMessageBubble({ sender: 'assistant', variant: 'filled', group: 'none' });
const a = avatar({ size: 'sm' });
const metaRow = stack({
  direction: 'row',
  gap: 'xs',
  align: 'center',
  justify: 'start',
  wrap: 'nowrap',
});
const time = textBlock({ size: 'sm', tone: 'secondary' });
---

<div {...mergeProps(m.root)}>
  <div {...mergeProps(m.avatar)}>
    <span {...mergeProps(a.root)}>
      <span {...mergeProps(a.initials)} role="img" aria-label="Navi">N</span>
    </span>
  </div>
  <div {...mergeProps(m.content)}>
    <div {...mergeProps(m.header)}>
      <span {...mergeProps(m.name)}>Navi</span>
    </div>
    <div {...mergeProps(bubble.root)}>Hello! How can I help?</div>
    <div {...mergeProps(m.metadata)}>
      <div {...mergeProps(metaRow)}>
        <time
          {...mergeProps(time)}
          datetime="2026-07-19T21:35:00.000Z"
          title="Jul 19, 2026, 2:35 PM">2:35 PM</time
        >
      </div>
    </div>
  </div>
</div>
