---
import { checkbox } from '@var-ui/core';
import { mergeProps } from '../../../lib/mergeProps';

const cb = checkbox();
---

<label {...mergeProps(cb.root)}>
  <input type="checkbox" class="sr-only" />
  <span {...mergeProps(cb.box)}></span>
  <span {...mergeProps(cb.label)}>Accept terms</span>
</label>

<style>
  .sr-only {
    position: absolute;
    width: 1px;
    height: 1px;
    padding: 0;
    margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }
</style>
