---
import { combobox } from '@var-ui/core';
import { mergeProps } from '../../../lib/mergeProps';

const cb = combobox();
---

<div {...mergeProps(cb.root)}>
  <label {...mergeProps(cb.label)} for="fruit-typeahead">Fruit</label>
  <div {...mergeProps(cb.inputWrapper)}>
    <input
      {...mergeProps(cb.input)}
      id="fruit-typeahead"
      type="text"
      placeholder="Search…"
      role="combobox"
      aria-autocomplete="list"
    />
  </div>
</div>
