---
import {
  avatar,
  button,
  chatComposer,
  chatLayout,
  chatMessage,
  chatMessageBubble,
  chatMessageList,
  resolveButtonProps,
} from '@var-ui/core';
import { mergeProps } from '../../../lib/mergeProps';

// No @var-ui/astro ChatLayout — static core-recipe shell (scroll/send are React-only).
const l = chatLayout();
const list = chatMessageList({ density: 'balanced' });
const m = chatMessage({ sender: 'assistant' });
const bubble = chatMessageBubble({ sender: 'assistant', variant: 'filled', group: 'none' });
const a = avatar({ size: 'sm' });
const c = chatComposer();
const send = button(resolveButtonProps({ intent: 'primary' }));
---

<div style="height: 280px">
  <div {...mergeProps(l.root)}>
    <div {...mergeProps(l.messageArea)}>
      <div {...mergeProps(list.root)} role="log" aria-live="polite">
        <div {...mergeProps(list.inner)}>
          <div {...mergeProps(m.root)}>
            <div {...mergeProps(m.avatar)}>
              <span {...mergeProps(a.root)}>
                <span {...mergeProps(a.initials)} role="img" aria-label="Assistant">A</span>
              </span>
            </div>
            <div {...mergeProps(m.content)}>
              <div {...mergeProps(m.header)}>
                <span {...mergeProps(m.name)}>Assistant</span>
              </div>
              <div {...mergeProps(bubble.root)}>Hello! How can I help?</div>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div {...mergeProps(l.dock)}>
      <div {...mergeProps(c.root)}>
        <div {...mergeProps(c.inputRow)}>
          <textarea
            {...mergeProps(c.input)}
            rows="1"
            placeholder="Type a message…"
            aria-label="Message"
            disabled></textarea
          >
        </div>
        <div {...mergeProps(c.actions)}>
          <button type="button" {...mergeProps(send)} aria-label="Send message" disabled>
            <svg
              viewBox="0 0 24 24"
              width="16"
              height="16"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <path d="M12 19V5M5 12l7-7 7 7"></path>
            </svg>
          </button>
        </div>
      </div>
    </div>
  </div>
</div>
