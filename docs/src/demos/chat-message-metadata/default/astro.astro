---
import { stack, textBlock } from '@var-ui/core';
import { mergeProps } from '../../../lib/mergeProps';

// No @var-ui/astro ChatMessageMetadata — Timestamp + status via core recipes.
const row = stack({
  direction: 'row',
  gap: 'xs',
  align: 'center',
  justify: 'start',
  wrap: 'nowrap',
});
const time = textBlock({ size: 'sm', tone: 'secondary' });
---

<div {...mergeProps(row)}>
  <time {...mergeProps(time)} datetime="2026-07-19T21:35:00.000Z" title="Jul 19, 2026, 2:35 PM"
    >2:35 PM</time
  >
  Read
</div>
