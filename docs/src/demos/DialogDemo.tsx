'use client';

import { Dialog } from '@var-ui/react';

export function DialogDemo() {
  return (
    <Dialog
      triggerLabel="Open dialog"
      title="Example dialog"
      description="Modal overlay for focused tasks."
    />
  );
}
