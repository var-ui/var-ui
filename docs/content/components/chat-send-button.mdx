---
title: ChatSendButton
description: Send/stop button for a chat composer.
---

import { Demo } from '../../src/components/Demo';
import { ChatSendButtonDemo } from '../../src/demos/ChatSendButtonDemo';

# ChatSendButton

Send/stop button for a chat composer. Reuses the existing `Button` and `Icon` rather than a dedicated recipe. See [ChatComposer](/components/chat-composer) for the full composer wiring.

## Examples

### Default

<Demo code={`import { ChatSendButton } from '@var-ui/react';

<ChatSendButton isStreaming={isStreaming} onPress={send} onStop={cancel} />`}>
  <ChatSendButtonDemo />
</Demo>

## Props

<PropsTable slug="chat-send-button" />

## Accessibility

`aria-label` switches between "Send message" and "Stop generating" based on `isStreaming`.
