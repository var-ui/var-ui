---
title: Design tokens
description: Semantic tokens that power var-ui components and layouts.
---

# Design tokens

var-ui exposes semantic tokens for color, spacing, typography, and elevation. Components reference tokens instead of hard-coded values so themes can swap palettes without touching component code.

Token reference tables will be generated from the core package in a follow-up.
